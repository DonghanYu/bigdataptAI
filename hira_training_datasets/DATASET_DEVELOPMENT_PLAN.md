# HIRA 주제별 학습 데이터셋 개발 계획안

## 📊 현재 상황 분석

### 기존 데이터
- **소스**: `hira_crawler/output/hira_data_from_yaml_*.json`
- **규모**: 323개 Q&A 쌍
- **구조**:
  - 5개 메뉴 (service_intro, healthcare_bigdata, medical_statistics, public_data, customer_support)
  - 53개 세부 주제
  - 질문 평균 15.4자, 답변 평균 80.8자

### 목표
**323개 → 5,000~10,000개 고품질 학습 데이터 생성**

---

## 🎯 Phase 1: 데이터 분석 및 설계 (1일)

### 1.1 주제 분류 체계 구축
```python
주제 계층 구조:
├── Level 1: 메뉴 (5개)
│   ├── service_intro (서비스 소개)
│   ├── healthcare_bigdata (보건의료빅데이터)
│   ├── medical_statistics (의료통계정보)
│   ├── public_data (공공데이터)
│   └── customer_support (고객지원)
│
├── Level 2: 주제 (53개)
│   ├── code_lookup (코드 조회)
│   ├── data_request (자료 신청)
│   ├── customized_analysis (맞춤형 분석)
│   └── ...
│
└── Level 3: 세부 주제 (자동 생성)
    ├── 상병코드 조회 방법
    ├── KCD 코드 설명
    └── ...
```

### 1.2 현재 데이터 통계 분석
```bash
스크립트: analyze_current_data.py
출력:
- 메뉴별 Q&A 분포
- 주제별 Q&A 밀도
- 질문 패턴 분석 (시작 단어, 길이)
- 답변 패턴 분석 (구조, 길이)
- 부족한 주제 식별
```

### 1.3 타겟 데이터셋 정의
| 메뉴 | 현재 Q&A | 목표 Q&A | 증강 배율 |
|------|----------|----------|-----------|
| 서비스 소개 | 68 | 1,500 | 22x |
| 보건의료빅데이터 | 108 | 3,000 | 28x |
| 의료통계정보 | 64 | 2,000 | 31x |
| 공공데이터 | 40 | 1,500 | 38x |
| 고객지원 | 43 | 2,000 | 47x |
| **합계** | **323** | **10,000** | **31x** |

---

## 🚀 Phase 2: 데이터 증강 전략 (2-3일)

### 2.1 규칙 기반 변형 (Strategy 1)
**목표: 323개 → 2,000개 (6x)**

#### 변형 기법
```python
1. 어미 변형 (Ending Variations)
   - 하나요? → 해요? / 할까요? / 하죠?
   - 인가요? → 이에요? / 일까요? / 이죠?

2. 질문 형식 변형 (Question Format)
   - 직접 질문: "상병코드 조회 방법은?"
   - 간접 질문: "상병코드 어떻게 조회하나요?"
   - 요청형: "상병코드 조회 방법 알려주세요"
   - 의문형: "상병코드는 어디서 확인하나요?"

3. 동의어 치환 (Synonym Replacement)
   - 신청 ↔ 요청 ↔ 등록
   - 방법 ↔ 절차 ↔ 과정
   - 확인 ↔ 조회 ↔ 검색
   - 데이터 ↔ 자료 ↔ 정보

4. 조사 변형 (Particle Variation)
   - 은/는, 이/가, 을/를 교체

5. 어순 변경 (Word Order)
   - "HIRA 데이터 신청 방법" → "데이터 신청은 HIRA에서 어떻게"

6. 축약/확장 (Abbreviation/Expansion)
   - "API 키 발급 방법" → "API 인증키는 어떻게 발급받나요?"
```

**구현**: `generators/hira_rule_based_augmentor.py`

### 2.2 주제 기반 생성 (Strategy 2)
**목표: 2,000개 → 5,000개 (추가 3,000개)**

#### 주제별 질문 템플릿
```yaml
templates:
  how_to:
    - "{주제} 어떻게 하나요?"
    - "{주제} 방법은?"
    - "{주제} 절차를 알려주세요"

  what_is:
    - "{용어}가 뭔가요?"
    - "{용어}에 대해 설명해주세요"
    - "{용어}의 의미는?"

  where:
    - "{기능}은 어디서 하나요?"
    - "{메뉴}는 어느 곳에서 찾나요?"

  comparison:
    - "{A}와 {B}의 차이는?"
    - "{A}와 {B} 어떻게 다른가요?"
```

**구현**: `generators/hira_template_based_generator.py`

### 2.3 GPT 기반 증강 (Strategy 3, 선택적)
**목표: 5,000개 → 10,000개 (추가 5,000개)**

```python
GPT-4 프롬프트:
"""
다음 HIRA 관련 Q&A를 바탕으로 5개의 유사하지만 다른 질문을 생성해주세요.
원본 질문: {question}
원본 답변: {answer}

조건:
1. 질문의 의도는 동일하되 표현 방식을 다르게
2. 구어체/문어체 혼합
3. 존댓말/반말 혼합
4. 질문 길이 10-30자 유지
"""
```

**비용 예상**:
- GPT-4: 5,000개 생성 시 약 $10-20
- GPT-3.5-turbo: 5,000개 생성 시 약 $2-5

**구현**: `generators/hira_gpt_augmentor.py` (선택적)

---

## 📐 Phase 3: 데이터셋 구조 설계 (1일)

### 3.1 표준 데이터 스키마
```json
{
  "id": "hira_service_intro_001",
  "instruction": "상병코드는 어떻게 조회하나요?",
  "input": "",
  "output": "상병코드(KCD 코드)는 '서비스 소개 > 용어설명 > ...",
  "metadata": {
    "menu": "service_intro",
    "menu_name": "서비스 소개",
    "topic": "code_lookup",
    "topic_name": "코드 조회",
    "keywords": ["상병코드", "KCD", "조회"],
    "generation_method": "manual|rule_based|template|gpt",
    "source_id": "original_qa_001",
    "difficulty": "easy|medium|hard",
    "question_type": "how_to|what_is|where|comparison",
    "created_at": "2025-11-18T...",
    "quality_score": 0.95
  }
}
```

### 3.2 주제별 데이터셋 분할
```
output/
├── by_menu/
│   ├── service_intro_train.json (1,200개)
│   ├── service_intro_val.json (150개)
│   ├── service_intro_test.json (150개)
│   ├── healthcare_bigdata_train.json (2,400개)
│   ├── ...
│
├── by_topic/
│   ├── code_lookup_train.json
│   ├── data_request_train.json
│   ├── ...
│
├── by_difficulty/
│   ├── easy_train.json
│   ├── medium_train.json
│   ├── hard_train.json
│
└── combined/
    ├── hira_full_train.json (8,000개)
    ├── hira_full_val.json (1,000개)
    ├── hira_full_test.json (1,000개)
    └── hira_full_train.jsonl (LoRA 학습용)
```

---

## 🔨 Phase 4: 구현 (3-5일)

### 4.1 데이터 증강 파이프라인
```python
# generators/hira_dataset_builder.py

class HIRADatasetBuilder:
    def __init__(self, source_json):
        self.source_data = load_json(source_json)
        self.augmented_data = []

    def build(self):
        # 1. 규칙 기반 증강
        self.augmented_data.extend(
            self.rule_based_augment(multiplier=6)
        )

        # 2. 템플릿 기반 생성
        self.augmented_data.extend(
            self.template_based_generate(target=3000)
        )

        # 3. GPT 기반 증강 (선택적)
        if self.use_gpt:
            self.augmented_data.extend(
                self.gpt_augment(target=5000)
            )

        # 4. 품질 필터링
        self.augmented_data = self.quality_filter(
            min_score=0.7
        )

        # 5. 중복 제거
        self.augmented_data = self.deduplicate()

        # 6. 메타데이터 추가
        self.augmented_data = self.add_metadata()

        return self.augmented_data
```

### 4.2 품질 검증 시스템
```python
# validators/quality_checker.py

class QualityChecker:
    def check(self, qa_pair):
        scores = {
            "length": self._check_length(qa_pair),
            "grammar": self._check_grammar(qa_pair),
            "relevance": self._check_relevance(qa_pair),
            "diversity": self._check_diversity(qa_pair),
            "consistency": self._check_consistency(qa_pair)
        }

        return {
            "overall_score": sum(scores.values()) / len(scores),
            "details": scores,
            "passed": all(s >= 0.7 for s in scores.values())
        }
```

### 4.3 통계 및 시각화
```python
# analyzers/dataset_analyzer.py

class DatasetAnalyzer:
    def analyze(self, dataset):
        return {
            "total_count": len(dataset),
            "menu_distribution": self._menu_dist(),
            "topic_distribution": self._topic_dist(),
            "question_length_stats": self._q_length_stats(),
            "answer_length_stats": self._a_length_stats(),
            "generation_method_dist": self._gen_method_dist(),
            "quality_score_dist": self._quality_dist(),
            "diversity_score": self._diversity_score()
        }

    def visualize(self):
        # 메뉴별 분포 차트
        # 주제별 밀도 히트맵
        # 길이 분포 히스토그램
        # 품질 점수 분포
```

---

## 🎯 Phase 5: 검증 및 최적화 (2일)

### 5.1 품질 검증 체크리스트
- [ ] 중복 제거 (BLEU 유사도 < 0.95)
- [ ] 길이 검증 (질문: 5-50자, 답변: 20-300자)
- [ ] 문법 검사 (한국어 맞춤법)
- [ ] 일관성 검증 (질문-답변 매칭)
- [ ] 다양성 검증 (어휘 다양성, 패턴 다양성)
- [ ] 균형성 검증 (주제별 균형)

### 5.2 샘플링 및 수동 검수
```python
# 각 메뉴에서 50개 랜덤 샘플링
# 품질 점수별 계층 샘플링
# 수동 검수 후 점수 부여
# 낮은 품질 데이터 제거 또는 수정
```

### 5.3 A/B 테스트 준비
```python
# 작은 데이터셋 vs 큰 데이터셋
# 규칙 기반 vs GPT 기반
# 주제별 균형 vs 불균형
```

---

## 📊 Phase 6: 최종 출력 (1일)

### 6.1 최종 데이터셋 생성
```bash
hira_training_datasets/
├── v1.0/
│   ├── metadata.json
│   ├── statistics.json
│   ├── quality_report.json
│   │
│   ├── full/
│   │   ├── train.json (8,000개)
│   │   ├── train.jsonl
│   │   ├── val.json (1,000개)
│   │   └── test.json (1,000개)
│   │
│   ├── by_menu/
│   │   ├── service_intro/
│   │   │   ├── train.json
│   │   │   ├── val.json
│   │   │   └── test.json
│   │   └── ...
│   │
│   └── specialized/
│       ├── easy_qa.json
│       ├── medium_qa.json
│       ├── hard_qa.json
│       └── key_concepts.json
```

### 6.2 문서화
```markdown
- README.md: 데이터셋 소개
- DATA_SCHEMA.md: 스키마 설명
- GENERATION_METHOD.md: 생성 방법론
- QUALITY_REPORT.md: 품질 보고서
- USAGE_GUIDE.md: 사용 가이드
```

---

## 📅 전체 일정 (10-14일)

| Phase | 작업 | 소요 시간 | 담당 |
|-------|------|----------|------|
| 1 | 데이터 분석 및 설계 | 1일 | 기획 |
| 2 | 규칙 기반 증강 구현 | 2일 | 개발 |
| 2 | 템플릿 기반 생성 구현 | 2일 | 개발 |
| 2 | GPT 증강 (선택) | 1일 | 개발 |
| 3 | 데이터셋 구조 설계 | 1일 | 기획 |
| 4 | 파이프라인 구현 | 3일 | 개발 |
| 5 | 품질 검증 | 2일 | QA |
| 6 | 최종 출력 및 문서화 | 1일 | 문서 |
| **합계** | | **10-14일** | |

---

## 💰 예산 (GPT 사용 시)

| 항목 | 수량 | 단가 | 비용 |
|------|------|------|------|
| GPT-4 API | 5,000개 | $0.003/개 | $15 |
| GPT-3.5 API | 5,000개 | $0.001/개 | $5 |
| 품질 검증 (GPT) | 1,000개 | $0.002/개 | $2 |
| **합계 (GPT-4)** | | | **$17** |
| **합계 (GPT-3.5)** | | | **$7** |

**비용 절감 옵션**: 규칙+템플릿만 사용 → **$0**

---

## 🎯 예상 성과

### 데이터 규모
- **현재**: 323개 Q&A
- **목표 (규칙+템플릿)**: 5,000개 Q&A
- **목표 (GPT 포함)**: 10,000개 Q&A

### 품질 지표
- 중복률: < 5%
- 품질 점수: > 0.85
- 다양성 점수: > 0.80
- 일관성: > 0.90

### 활용도
- LLM 파인튜닝: LoRA, Full Fine-tuning
- RAG 지식베이스
- 챗봇 학습 데이터
- 검색 엔진 학습

---

## 🚀 다음 단계 제안

### Option A: 빠른 프로토타입 (2-3일)
1. 규칙 기반 증강만 구현
2. 323개 → 2,000개 생성
3. 품질 검증 후 즉시 LoRA 학습

### Option B: 완전한 데이터셋 (7-10일)
1. 규칙 + 템플릿 증강
2. 323개 → 5,000개 생성
3. 철저한 품질 검증
4. 주제별 분할 및 문서화

### Option C: 프리미엄 데이터셋 (10-14일)
1. 규칙 + 템플릿 + GPT 증강
2. 323개 → 10,000개 생성
3. 다단계 품질 검증
4. 완전한 문서화 및 배포 준비

---

**추천**: Option B (완전한 데이터셋)
- 비용: $0
- 품질: 높음
- 시간: 합리적 (7-10일)
- 확장성: 우수

어떤 옵션으로 진행하시겠습니까?
